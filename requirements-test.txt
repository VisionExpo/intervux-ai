# Test dependencies for Intervux AI
# Install with: pip install -r requirements-test.txt --break-system-packages
#
# These are intentionally kept separate from backend/requirements.txt so
# production images don't carry test tooling.

# Core test runner
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# HTTP client used by FastAPI TestClient (async support)
httpx==0.27.0

# Async test utilities
anyio[trio]==4.4.0

# In-process SQLite for test isolation (no Postgres needed in CI)
# sqlite3 is part of the stdlib; no extra package needed.

# Mocking
# unittest.mock is stdlib; no extra package needed.
